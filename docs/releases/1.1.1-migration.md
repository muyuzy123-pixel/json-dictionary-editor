# 1.1.1 增量合并差异

基线是现有公开仓库 `main` 提交 `7d7ab5be545357aa49a0f499cb76822f65f382f4`，在独立 `codex/release-1.1.1` 分支上整理。本地开发交付中的两端产品源码已更新为 1.1.1；本次按明确文件清单逐项导入及适配，没有用旧源码目录覆盖仓库。

| 范围 | 合入的 1.1.1 内容 | 仓库既有实现的保留与适配 |
|---|---|---|
| macOS | 双语界面与语言资源、空容器搜索判断、中文目录缺失时的自检、英文示例及 1.1.1 (3) 元数据 | 保留原 SwiftUI 文档/保存语义、Bundle ID 和固定 ICNS；direct-swiftc 增加显式资源访问与本地化 bundle，分开无签名构建和 ad-hoc 暂存签名 |
| Windows | 双语语言表、搜索别名及相关测试、英文示例、1.1.1 双语版本资源 | 保留新 ICO、分隔条既有实现、安全保存、CMake 仅核心入口、LLVM-MinGW 来源/Loaded 归档/许可检查；更新版本、`uuid` 链接与输入清单 |
| 构建和验证 | 两端文案校验器、Mac 双语目录/应用名称资源、Windows 英文说明 | 继续使用仓库 `tests/` 共享数据样例与原脚本；从实际最终包核对资源和 CLI |
| 附件 CI | 1.1.1 版本与状态输入 | 保持 `contents: read`、下载最终附件、无 checkout/重建/发布写权限；可分别验证已发布预发布或正式版 |
| 文档 | 发行说明比较 `preview-1 → v1.1.1`，英文指南及本地候选记录 | 旧 `preview-1` 标签、附件、`PREVIEW_RELEASE.md` 和原验证证据完整保留 |

未导入本地开发目录的旧 `app.ico`、签名打包脚本、已编译 EXE/App、工具链、缓存、完整会话日志或私人样例；Mac 固定 `AppIcon.icns` 也未重新渲染。导入前后图标 SHA-256 分别为 Mac `e0a3b35e53ee2fe28bd95b988e6d9555a4902361c3338051c3667e92245ea53d`、Windows `e635dbe42f45fb5321707439e05db95aae9c02f11c595ee73dd9873353cccae9`。

保留与版本新增属于不同时间线：Windows 分隔条修复在 `preview-1` 的 1.0.1 源码内已有；1.1.1 没有重新设计分隔条。语言切换代码沿用本地 1.1.0 既有行为，仅适配语言资源的仓库直编布局。Mac 历史 AX 直接赋值保存挂起也没有被本次业务合并直接认定为已修复。
